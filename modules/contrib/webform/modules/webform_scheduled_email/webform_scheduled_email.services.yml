services:
  webform_scheduled_email.manager:
    class: Drupal\webform_scheduled_email\WebformScheduledEmailManager
    arguments: ['@datetime.time', '@database', '@config.factory', '@logger.factory', '@entity_type.manager', '@webform.token_manager']
